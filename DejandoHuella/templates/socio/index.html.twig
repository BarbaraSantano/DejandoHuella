{% extends 'base.html.twig' %}

{% block title %}Hazte Socia/o{% endblock %}

{% block body %}


        <div class="row h-100 justify-content-center align-items-center">
            <div class="col-6 shadow bg-white rounded my-3 p-5">
                <h1 id="prueba">Hazte Socia/o</h1>

                {% for message in app.flashes('exito') %}
                    <div class="alert alert-success">
                        {{ message }}
                    </div>
                {% endfor %}

                {{form(form_socio)}}

                <a id="botonCrear"  href="{{'http://localhost:3000/' }}">Volver</a>
            </div>
        </div>

{% endblock %}
