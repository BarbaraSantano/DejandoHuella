<form method="post" action="{{ path('app_animal_delete', {'id': animal.id}) }}" onsubmit="return confirm('¿Estás seguro de que quieres eliminarlo?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ animal.id) }}">
    <button  id="botonCrear">Eliminar</button>
</form>
