<form method="post" action="{{ path('app_gato_delete', {'id': gato.id}) }}" onsubmit="return confirm('¿Estás seguro de que quieres eliminarlo?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gato.id) }}">
    <button id="botonCrear">Eliminar</button>
</form>
